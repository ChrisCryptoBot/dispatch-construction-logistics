# =============================================================================
# PHASE 1 ENVIRONMENT VARIABLES
# =============================================================================
# Copy this to .env.local and configure for your environment
# All feature flags are OFF by default (schema preparation only)

# =============================================================================
# DATABASE
# =============================================================================
DATABASE_URL="postgresql://user:password@localhost:5432/dispatch_dev"

# =============================================================================
# FEATURE FLAGS (Phase 1) - ALL OFF BY DEFAULT
# =============================================================================
# Enable these ONE AT A TIME after PR2 (services) is merged

# Client-side flags (Vite)
VITE_FF_CREDIT_GUARDRAIL=false
VITE_FF_BID_LOCK=false
VITE_FF_SMS_FALLBACK=false
VITE_FF_DISPATCH_AUDIT=false
VITE_FF_GEOFENCE=false
VITE_FF_LINE_ITEMS=false
VITE_FF_DAMAGE_CAPTURE=false
VITE_FF_PAYMENT_RETRY=false

# Server-side flags (Node)
FF_CREDIT_GUARDRAIL=false
FF_BID_LOCK=false
FF_SMS_FALLBACK=false
FF_DISPATCH_AUDIT=false
FF_GEOFENCE=false
FF_LINE_ITEMS=false
FF_DAMAGE_CAPTURE=false
FF_PAYMENT_RETRY=false

# =============================================================================
# INFRASTRUCTURE (For PR2 - Services)
# =============================================================================
# Leave empty for now - will be configured in PR2

# Redis (for locks & job queues)
REDIS_URL=

# Job Queue
JOB_QUEUE_URL=

# Document Storage
DOC_BUCKET=
PUBLIC_ASSET_CDN=

# Encryption
KMS_KEY_ID=

# =============================================================================
# EXTERNAL SERVICES (For PR2 - Services)
# =============================================================================
# Leave empty for now - will be configured when ready to integrate

# SMS Provider (Twilio, etc.)
SMS_PROVIDER_KEY=
SMS_PROVIDER_SECRET=
SMS_FROM_NUMBER=

# Email Provider (SendGrid, Mailgun, etc.)
EMAIL_PROVIDER_KEY=
EMAIL_FROM_ADDRESS=

# Payment Provider (Stripe, etc.)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# Maps/Geolocation (Google Maps, etc.)
GOOGLE_MAPS_API_KEY=

# =============================================================================
# SECURITY
# =============================================================================
JWT_SECRET=your-secure-secret-key-here
SESSION_SECRET=your-session-secret-here

# =============================================================================
# MONITORING (Optional)
# =============================================================================
SENTRY_DSN=
LOG_LEVEL=info

# =============================================================================
# NOTES
# =============================================================================
# PR1: Only DATABASE_URL is required
# PR2: Add REDIS_URL, KMS_KEY_ID, SMS_*, EMAIL_*
# PR3+: Enable feature flags ONE AT A TIME for testing



