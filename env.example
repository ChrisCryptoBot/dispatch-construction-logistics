# Database
DATABASE_URL="postgresql://user:password@localhost:5432/construction_logistics"

# JWT Secret
JWT_SECRET="your-super-secret-jwt-key-here"

# API Configuration
PORT=3000
NODE_ENV=development

# External Services (for later integration)
STRIPE_SECRET_KEY="sk_test_..."
TWILIO_ACCOUNT_SID="AC..."
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
GOOGLE_MAPS_API_KEY="your-google-maps-api-key"

# Redis (for sessions and caching)
REDIS_URL="redis://localhost:6379"

# S3 Storage (for documents)
AWS_ACCESS_KEY_ID="your-aws-access-key"
AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
AWS_REGION="us-east-1"
S3_BUCKET_NAME="dispatch-documents"

# Email Verification (Optional - uses mock mode if not set)
POSTMARK_TOKEN="pm_************************"
EMAIL_FROM="no-reply@superiorone.com"
VERIFICATION_TTL_MINUTES=15
VERIFICATION_MAX_ATTEMPTS=5
VERIFICATION_RESEND_COOLDOWN_SECONDS=60
VERIFICATION_DAILY_RESEND_MAX=5

# reCAPTCHA v3 (Optional - uses mock mode if not set)
RECAPTCHA_SECRET="6Le***********************************"
RECAPTCHA_SITE_KEY="6Le***********************************"

# Application URLs
APP_BASE_URL="http://localhost:5173"
API_BASE_URL="http://localhost:3000"

# Server Configuration (Unit 3 Consolidation - STAGING)
USE_OPTIMIZED_ENTRY=false
USE_OPTIMIZED_FEATURES=false
ENABLE_COMPRESSION=true
ENABLE_METRICS=true
ENABLE_REQUEST_LOGGING=true
NODE_ENV=staging